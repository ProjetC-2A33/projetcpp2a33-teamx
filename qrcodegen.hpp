// qrcodegen.hpp - QR Code generator (based on the public-domain work by Nayuki)
// This header declares a minimal wrapper compatible with the Nayuki QR Code generator.

#ifndef QRCODEGEN_HPP
#define QRCODEGEN_HPP

#include <vector>
#include <string>

/*
 * Minimal public-domain QR code generator C++ header based on Nayuki's qrcodegen library.
 * For full features and compliance see: https://www.nayuki.io/page/qr-code-generator-library
 * This header provides the essential QrCode API used by the application:
 *   - QrCode::encodeText(text, ecl)
 *   - getSize(), getModule(x, y)
 */

#include <vector>
#include <string>

// qrcodegen header removed — kept as placeholder only. The project has moved away
// from storing or generating QR codes locally and this header is left for history.

namespace qrcodegen {
    class QrCode {}; // placeholder
}

#endif // QRCODEGEN_HPP
